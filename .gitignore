/target

# Local dev secrets — never committed, see .env.example
.env

# Example CLI's persisted session state (tokens + a DPoP private key)
.auth-cli-state.json

# Conversation exports from /export — not project source
*.txt

# Terraform local state and cache — may contain resource details, never committed
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
